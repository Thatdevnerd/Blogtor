<div class="form-container">
    <div class="blog-edit-wrapper">
{#        <div class="form-title">#}
{#            <h1>Edit Blogs</h1>#}
{#        </div>#}

        {{ form_start(form) }}
        {{ form_widget(form, {'attr': {'class': "blog-edit-form"}}) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
        {{ include('admin/_delete_form.html.twig') }}
        {{ form_end(form) }}
    </div>
</div>